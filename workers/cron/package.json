{
  "name": "snipotter-cron-worker",
  "version": "0.6.0",
  "private": true,
  "description": "Nightly Cloudflare cron that pulls install + rating + review snapshots from every connected source (GitHub, Microsoft Store, Google Play, Cloudflare Analytics) into Supabase.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.6.3",
    "wrangler": "^4.0.0"
  }
}
