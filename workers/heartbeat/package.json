{
  "name": "snipotter-heartbeat-worker",
  "version": "0.6.0",
  "private": true,
  "description": "Cloudflare Worker that accepts /heartbeat POSTs from Snipotter desktop + mobile apps and writes to the heartbeats table in Supabase. Origin-locked, anonymous, no PII.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.6.3",
    "wrangler": "^4.0.0"
  }
}
